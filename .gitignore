node_modules/
dist/
dev-dist/
*.local
.DS_Store
.vite/

# Vendored OCR runtime — regenerated by `npm run prepare:ocr`, kept out of git
# so the repo stays small (~7 MB of wasm + language data).
public/tesseract/

# Debug output from the calibration harnesses
.debug/

# Test photos stay local: magazine pages are copyrighted, and the repo is meant
# to stay small. Drop your own shots here to calibrate (see README).
fixtures/
